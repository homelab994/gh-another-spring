spring:
  application:
    name: gh-another-spring
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:http://gh-config-server:9090
server:
  port: 9001

another:
  greeting:
    message: ${greeting.message}
